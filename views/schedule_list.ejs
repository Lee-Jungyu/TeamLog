<!DOCTYPE html>
<html>
    <head>
        <title>Friend List</title>
    </head>
    <body>
        <h1>일정 목록 입니다.</h1>
        <h3>일정 목록</h3>
        <table>
            <tr>
                <th>번호</th>
                <th>일정 이름</th>
                <th>시작 일시</th>
                <th>일정 관리</th>
            </tr>
            <% for (var i = 0; i < joinedScheduleList.length; i++) { %>
                <tr>
                    <td><label> <%= i + 1 %> </label> </td>
                    <td><label> <%= joinedScheduleList[i].schedule_list[0].name %> </label></td>
                    <td><label> <%= joinedScheduleList[i].schedule_list[0].start_date %> </label></td>
                    <td>
                        <form method="get" action="/manage_schedule">
                            <input type="hidden" name="schedule_id" value="<%= joinedScheduleList[i].schedule_list[0].id %>">
                            <button type="submit"> 관리 </button>
                        </form>
                    </td>
                </tr>
            <% } %>
        </table>

        <h3>초대된 일정 목록</h3>
        <table>
            <tr>
                <th>번호</th>
                <th>일정 이름</th>
                <th>시작 일시</th>
                <th colspan="2">일정 관리</th>
            </tr>
            <% for (var i = 0; i < invitedScheduleList.length; i++) { %>
                <tr>
                    <td><label> <%= i + 1 %> </label> </td>
                    <td><label> <%= invitedScheduleList[i].schedule_list[0].name %> </label></td>
                    <td><label> <%= invitedScheduleList[i].schedule_list[0].start_date %> </label></td>
                    <td>
                        <form method="post" action="/api/scheduleUsers/invite/response/accept">
                            <input type="hidden" name="schedule_id" value="<%= invitedScheduleList[i].schedule_list[0].id %>">
                            <button type="submit"> 가입 </button>
                        </form>
                    </td>
                    <td>
                        <form method="post" action="/api/scheduleUsers/invite/response/reject">
                            <input type="hidden" name="schedule_id" value="<%= invitedScheduleList[i].schedule_list[0].id %>">
                            <button type="submit"> 거절 </button>
                        </form>
                    </td>
                </tr>
            <% } %>
        </table>
    </body>
</html>